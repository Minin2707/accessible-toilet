spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/toilets
    username: app
    password: app
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.yaml
    contexts: local

server:
  port: 8080

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG

app:
  s3:
    endpoint: http://localhost:9000
    bucket: toilets
    accessKey: minio
    secretKey: minio12345
    region: us-east-1
    presignTtlSeconds: 600
